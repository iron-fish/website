import React, { useEffect, useState } from "react";

export default [
    <span data-ty="input">ironfish blocks</span>,
    <span data-ty>
    {`
Show the block header of a requested hash

USAGE
  $ ironfish blocks:COMMAND

COMMANDS
  blocks:show  Show the block header of a requested hash
        `}
  </span>,
];
